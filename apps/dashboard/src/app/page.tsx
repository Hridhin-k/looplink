import type { ReactNode } from "react";

/**
 * Dashboard home route.
 *
 * Scaffold only — no product UI in this phase.
 *
 * @returns An empty page shell.
 */
export default function HomePage(): ReactNode {
  return null;
}
