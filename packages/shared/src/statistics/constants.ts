/**
 * Default rolling window used for requests-per-minute.
 */
export const DEFAULT_REQUESTS_PER_MINUTE_WINDOW_MS = 60_000;

/**
 * Default number of top endpoints returned.
 */
export const DEFAULT_TOP_ENDPOINTS_LIMIT = 10;
